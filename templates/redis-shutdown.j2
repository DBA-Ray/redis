#!/bin/bash
#
# Wrapper to close properly redis and sentinel
test x"$REDIS_DEBUG" != x && set -x

REDIS_CLI={{ prefixdir }}/bin/redis-cli
HOST=127.0.0.1
PORT={{ redis_port }}
PASS={{ redis_password }}

$REDIS_CLI -h $HOST -p $PORT -a "${PASS}" shutdown